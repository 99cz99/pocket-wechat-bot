# DNS 配置模板 — Android/Termux 环境
# start-bot.sh 会自动生成 /data/local/tmp/resolv.conf，通常不需要手动使用此文件
# 如果在非中国地区，建议替换为:
#   nameserver 8.8.8.8
#   nameserver 1.1.1.1
nameserver 114.114.114.114
nameserver 223.5.5.5
